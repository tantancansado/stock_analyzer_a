# Generado por XcodeGen desde project.yml — no se commitea: son ~3k líneas de
# pbxproj que dan conflicto en cada merge y se regeneran en un segundo.
StockAnalyzer.xcodeproj/

# Xcode
xcuserdata/
DerivedData/
*.xcuserstate
build/
